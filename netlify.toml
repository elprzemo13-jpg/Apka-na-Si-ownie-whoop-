[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"

# SPA: every path serves index.html, the router takes over
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
